import React from 'react';
import logo from '../public/favicon.ico';

const Logo: React.FC = () => (
  <img src={logo} alt="Goal Forge Logo" style={{ width: 40, height: 40 }} />
);

export default Logo;
